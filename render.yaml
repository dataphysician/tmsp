services:
  - type: web
    name: tmsp
    runtime: python
    plan: free
    buildCommand: pip install uv && uv sync --extra server && cd frontend && npm ci && npm run build
    startCommand: .venv/bin/uvicorn server.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
